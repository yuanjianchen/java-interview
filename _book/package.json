{
  "scripts": {
    "serve": "gitbook serve",
    "build": "gitbook build && rm -rf docs/* && cp -r _book/* docs/",
    "push": "npm run-script build && git add . && git commit -m 'xx' && git push"
  }
}