{
  "scripts": {
    "serve": "gitbook serve",
    "build": "gitbook build && rm -rf doc/* && cp -r _book/* doc/",
    "push": "git add . && git commit -m 'xx' && git push",
    "publish": "gitbook build && cd _book && git add . && git commit -m 'xx' && git push"
  }
}