{
  "scripts": {
    "serve": "gitbook serve",
    "build": "rm -rf docs/ _book/ && gitbook build  && cp -r _book/ docs/",
    "push": "npm run-script build && git add . && git commit -m 'xx' && git push"
  }
}