{
  "scripts": {
    "serve": "gitbook serve",
    "build": "gitbook build",
    "push": "git add . && git commit -m 'xx' && git push",
    "publish": "gitbook build && git add . && git commit -m 'xx' && git push"
  }
}